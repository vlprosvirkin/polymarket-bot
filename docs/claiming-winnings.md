# Клейм выигрышей на Polymarket

## 🔍 Как это работает

На Polymarket, когда рынок разрешается (resolve), выигрышные токены (YES или NO) автоматически становятся worth **$1.00 каждый**. Однако, при торговле через CLOB API напрямую (минуя веб-интерфейс), вам нужно **вручную обменять** эти токены на USDC через смарт-контракт `ConditionalTokens`.

### Автоматический клейм (через веб-интерфейс)

Если вы торгуете через веб-интерфейс Polymarket:
- ✅ Выигрыш **автоматически** переводится на ваш баланс USDC
- ✅ Никаких действий не требуется
- ✅ Средства доступны сразу после разрешения рынка

### Ручной клейм (через CLOB API)

Если вы торгуете напрямую через CLOB API (как в этом боте):
- ⚠️ Выигрышные токены остаются в вашем кошельке
- ⚠️ Нужно **вручную вызвать** `redeemPositions` в смарт-контракте
- ⚠️ Только после этого токены обмениваются на USDC

## 🚀 Использование скрипта

### Шаг 1: Проверка выигрышных позиций

Запустите скрипт для проверки:

```bash
npm run ts-node scripts/claim-winnings.ts
```

Скрипт:
1. ✅ Проверяет все ваши позиции
2. ✅ Находит разрешенные рынки (`closed = true`)
3. ✅ Определяет выигрышные токены (`winner = true`)
4. ✅ Показывает список позиций для клейма

**Пример вывода:**
```
🔍 ПРОВЕРКА И КЛЕЙМ ВЫИГРЫШЕЙ
======================================================================

👤 Адрес: 0x...
💰 Текущий баланс USDC: 1000.50 USDC

🔍 Поиск выигрышных позиций...

🎉 Найдено 2 выигрышных позиций:

   ✅ Yes в "Will Bitcoin reach $100k by 2025?..."
      Количество: 50.00 токенов (≈ $50.00)

   ✅ No в "Will Ethereum reach $10k by 2025?..."
      Количество: 20.00 токенов (≈ $20.00)
```

### Шаг 2: Автоматический клейм

Для автоматического клейма установите переменную окружения:

```bash
AUTO_CLAIM=true npm run ts-node scripts/claim-winnings.ts
```

Или добавьте в `.env`:
```env
AUTO_CLAIM=true
```

**⚠️ ВНИМАНИЕ:** Клейм нельзя отменить! Убедитесь, что вы хотите обменять токены на USDC.

### Шаг 3: Проверка результата

После клейма скрипт покажет:
- 💰 Баланс USDC до клейма
- 💰 Баланс USDC после клейма
- 🎉 Сумму заклеймленных средств

## 📋 Требования

1. **Разрешенные рынки**: Рынок должен быть закрыт (`closed = true`)
2. **Выигрышные токены**: У вас должны быть токены с `winner = true`
3. **Баланс токенов**: Токены должны быть в вашем кошельке (не проданы)
4. **MATIC для газа**: Нужно иметь MATIC в кошельке для оплаты транзакций

## 🔧 Как работает скрипт

1. **Получение разрешенных рынков**: Использует `client.getMarkets({ closed: true })`
2. **Проверка балансов**: Через смарт-контракт `ConditionalTokens.balanceOf()`
3. **Определение выигрышей**: Сравнивает токены с `winner = true` в разрешенных рынках
4. **Вызов redeemPositions**: Обменивает токены на USDC через смарт-контракт

## 💡 Важные замечания

### Когда рынок разрешается?

- Обычно в течение **2-24 часов** после завершения события
- Администрация Polymarket проверяет результат через независимых оракулов
- Статус меняется на `closed = true` и `winner` устанавливается для выигрышного токена

### Можно ли продать токены до разрешения?

✅ **Да!** Вы можете продать токены в любой момент до разрешения рынка по текущей рыночной цене. Это позволяет:
- Зафиксировать прибыль досрочно
- Минимизировать убытки
- Не ждать разрешения рынка

### Что если рынок еще не разрешен?

Если рынок еще не разрешен (`closed = false`):
- ❌ Клейм невозможен
- ✅ Вы можете продать токены на рынке
- ⏳ Нужно дождаться разрешения администрацией

### Где хранятся токены?

Токены хранятся **напрямую в вашем кошельке** (MetaMask, WalletConnect и т.д.) в смарт-контракте `ConditionalTokens`. Они не находятся на балансе Polymarket, поэтому веб-интерфейс не может автоматически их обменять.

## 🐛 Troubleshooting

### "Нет выигрышных позиций для клейма"

Возможные причины:
- Рынок еще не разрешен (`closed = false`)
- У вас нет выигрышных токенов (вы продали их или купили проигрышный исход)
- Токены уже были заклеймлены ранее

**Решение**: Проверьте через `client.getMarkets({ closed: true })` и `client.getTrades({})`

### "Failed to get balance for token"

Возможные причины:
- Токен не существует или был удален
- Проблемы с подключением к RPC провайдеру

**Решение**: Проверьте подключение к Polygon RPC и убедитесь, что токен валиден

### "Transaction failed"

Возможные причины:
- Недостаточно MATIC для газа
- Рынок еще не разрешен
- Токены уже были заклеймлены

**Решение**: 
- Убедитесь, что у вас есть MATIC в кошельке
- Проверьте статус рынка через API
- Проверьте историю транзакций на PolygonScan

## 📚 Дополнительные ресурсы

- [Polymarket CLOB API Docs](https://docs.polymarket.com)
- [ConditionalTokens Contract](https://polygonscan.com/address/0x4D97DCd97eC945f40cF65F87097ACe5EA0476045)
- [Polygon RPC](https://polygon-rpc.com)

