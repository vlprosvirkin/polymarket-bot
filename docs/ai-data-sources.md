# Откуда AI получает данные для вынесения вердикта?

## 🔍 Источники данных для AI

AI использует **два основных источника** данных:

1. **Явные данные** (передаются в промпт)
2. **Внутренние знания** (из обучения модели)

---

## 1. Явные данные (Что мы передаем AI)

### 📊 Текущая реализация в `AIMarketScorer`:

```typescript
// Данные из Polymarket API, которые передаются в промпт:
{
    question: "Will Team X win the championship?",
    description: "Championship game description...",
    yesPrice: 0.65,           // 65%
    noPrice: 0.35,           // 35%
    active: true,
    accepting_orders: true,
    neg_risk: false,
    daysToResolution: 7,     // Вычисляется
    category: "sports",
    tags: ["basketball", "NBA"]
}
```

### 📝 Что попадает в промпт:

```typescript
// Пример промпта, который отправляется AI:
const prompt = `
Analyze this prediction market:

**Market Question:** Will Team X win the championship?
**Description:** Championship game description...

**Market Data:**
- YES Token Price: 65.00%
- NO Token Price: 35.00%
- Active: true
- Accepting Orders: true
- NegRisk Market: false
- Days to Resolution: 7
- Category: sports
- Tags: basketball, NBA

**Trading Context:**
- Strategy: endgame
- Risk Tolerance: medium

Provide your analysis...
`;
```

### ✅ Что AI видит явно:

- **Вопрос рынка** - точная формулировка
- **Описание** - дополнительные детали (если есть)
- **Текущие цены** - рыночная оценка вероятности
- **Метаданные** - категория, теги, статусы
- **Время** - дни до разрешения
- **Контекст торговли** - стратегия, риск-толерантность

---

## 2. Внутренние знания AI (Что модель знает из обучения)

### 🧠 Знания модели:

Модели (GPT-4o, Gemini) обучены на огромном количестве данных **до определенной даты**:

#### GPT-4o:
- **Дата обучения**: До апреля 2024
- **Знает**: Исторические факты, закономерности, контекст до этой даты

#### Gemini:
- **Дата обучения**: Зависит от версии (обычно до момента релиза)
- **Знает**: Более свежие данные (если модель новая)

### 📚 Что включает внутренние знания:

1. **Исторические факты**
   - Результаты прошлых выборов
   - История команд/игроков
   - Паттерны событий

2. **Общие закономерности**
   - Как обычно работают prediction markets
   - Типичные паттерны цен
   - Риски разных категорий

3. **Контекстное понимание**
   - Понимание формулировок вопросов
   - Распознавание неоднозначностей
   - Оценка сложности событий

4. **Предметные знания**
   - Спортивная статистика (до даты обучения)
   - Политическая информация
   - Экономические индикаторы

### 💡 Пример использования внутренних знаний:

```
Рынок: "Will Team Lakers win NBA Finals 2024?"
Цена: 60%

AI анализирует:
1. Явные данные: Вопрос, цена 60%, категория sports
2. Внутренние знания:
   - История Lakers в плей-офф (до даты обучения)
   - Статистика команды (если есть в обучении)
   - Паттерны NBA Finals
   - Типичные вероятности для таких событий

Вердикт: "Price seems reasonable based on historical patterns,
          but consider recent form and injuries (if known)."
```

---

## 3. Что AI НЕ знает (Ограничения)

### ❌ Свежие данные после обучения:

1. **Свежие новости**
   - Если событие произошло вчера/сегодня
   - Если новость вышла после даты обучения
   - Если изменилась ситуация после обучения

2. **Реальные временные данные**
   - Текущие результаты игр
   - Актуальная статистика
   - Последние опросы

3. **Рыночные движения в реальном времени**
   - Как цена менялась сегодня
   - Текущий объем торговли (если не передаем)
   - Последние сделки

### ⚠️ Пример проблемы:

```
Рынок: "Will Player X score 30+ points tonight?"
Дата обучения модели: До апреля 2024
Текущая дата: Декабрь 2024
Игра: Сегодня вечером

AI НЕ знает:
- Что игрок X травмирован вчера
- Что игра перенесена
- Последние новости о составе команды

AI может только:
- Использовать историческую статистику (до апреля 2024)
- Общие паттерны
- Логические выводы из формулировки
```

---

## 4. Как AI использует данные для вердикта

### 🔄 Процесс анализа:

```
1. Получает явные данные из промпта
   └─> Question, prices, metadata

2. Применяет внутренние знания
   └─> Исторические факты, паттерны

3. Делает логические выводы
   └─> Сравнение с типичными случаями

4. Генерирует вердикт
   └─> Score, reasoning, risk factors
```

### 📊 Пример полного анализа:

**Рынок:**
```
Question: "Will Biden win 2024 US Presidential Election?"
Price: 45%
Category: Politics
Days to Resolution: 30
```

**Что AI анализирует:**

1. **Явные данные:**
   - Цена 45% означает рынок считает маловероятным
   - Политический рынок (высокий риск)
   - 30 дней до разрешения

2. **Внутренние знания (до апреля 2024):**
   - Исторические результаты выборов
   - Типичные паттерны политических рынков
   - Понимание формулировки вопроса

3. **Логические выводы:**
   - Цена 45% может быть занижена или завышена
   - Политические рынки волатильны
   - 30 дней - достаточно времени для изменений

4. **Вердикт:**
```json
{
  "score": 0.6,
  "reasoning": "Political markets are volatile. 
                Price at 45% seems low based on historical patterns,
                but current polling data (if known) should be considered.",
  "riskFactors": [
    "High volatility in political markets",
    "Potential for last-minute events",
    "30 days allows for significant price movements"
  ]
}
```

---

## 5. Как улучшить данные для AI

### Вариант A: Добавить больше явных данных

```typescript
// Расширенный промпт с дополнительными данными
const enhancedPrompt = `
Market: ${market.question}

**Market Data:**
- Current Price: ${market.price}%
- Volume: ${market.volume}  // Если доступно
- Spread: ${spread}%        // Вычисляется из orderbook
- Liquidity: ${liquidity}   // Из orderbook

**Historical Context:**
- Similar markets: ${similarMarkets.length} found
- Average resolution accuracy: ${avgAccuracy}%
- Price movement last 24h: ${priceChange}%

**Trading Metrics:**
- Order book depth: ${orderbookDepth}
- Best bid/ask: ${bestBid}/${bestAsk}
- Recent trades: ${recentTradesCount}

Analyze...
`;
```

### Вариант B: Добавить новости (внешние источники)

```typescript
// Добавление новостей в промпт
async buildEnhancedPrompt(market: Market): Promise<string> {
    // 1. Получаем новости
    const news = await this.newsService.getRecentNews(
        extractKeywords(market.question),
        { hours: 24 }
    );
    
    // 2. Добавляем в промпт
    let prompt = `Market: ${market.question}\n\n`;
    
    if (news.length > 0) {
        prompt += `**Recent News:**\n`;
        news.slice(0, 5).forEach(article => {
            prompt += `- ${article.title}\n`;
            prompt += `  Summary: ${article.summary}\n`;
            prompt += `  Sentiment: ${article.sentiment}\n\n`;
        });
    }
    
    prompt += `**Market Data:**\n`;
    // ... остальные данные
    
    return prompt;
}
```

### Вариант C: Добавить исторические данные

```typescript
// Добавление истории похожих рынков
const similarMarkets = await this.findSimilarMarkets(market);
const historicalData = await this.analyzeHistoricalMarkets(similarMarkets);

prompt += `
**Historical Context:**
- Similar markets analyzed: ${similarMarkets.length}
- Average accuracy: ${historicalData.avgAccuracy}%
- Typical price movement: ${historicalData.avgMovement}%
- Resolution patterns: ${historicalData.patterns}
`;
```

---

## 6. Сравнение источников данных

### Текущая реализация:

| Источник | Что передается | Что AI знает |
|----------|---------------|--------------|
| **Явные данные** | Question, price, category, tags, days | Все что передаем |
| **Внутренние знания** | Ничего явно | Исторические факты до даты обучения |
| **Новости** | ❌ Нет | ❌ Только если в обучении |

### С улучшениями (предложение):

| Источник | Что передается | Что AI знает |
|----------|---------------|--------------|
| **Явные данные** | Question, price, category, tags, days, **orderbook, volume** | Расширенный набор данных |
| **Внутренние знания** | Ничего явно | Исторические факты до даты обучения |
| **Новости** | ✅ Последние 24 часа (через API) | ✅ Свежие новости |
| **История** | ✅ Похожие рынки и их результаты | ✅ Паттерны разрешений |

---

## 7. Рекомендации

### Для текущей реализации:

✅ **Используем то, что есть:**
- Данные из Polymarket API (question, description, prices)
- Внутренние знания AI модели
- Метаданные (category, tags)

⚠️ **Ограничения:**
- Нет свежих новостей
- Нет реальных временных данных
- Зависит от даты обучения модели

### Для улучшенной реализации:

🎯 **Можно добавить:**
1. **Orderbook данные** - ликвидность, спред (уже есть в PolymarketDataService)
2. **Новостной сервис** - NewsAPI, SerpAPI для свежих новостей
3. **Исторические данные** - анализ похожих рынков из прошлого
4. **Текущие метрики** - объем торговли, активность

📊 **Приоритет:**
1. **Высокий**: Orderbook данные (уже доступны)
2. **Средний**: Новостной сервис (для свежих данных)
3. **Низкий**: Исторические данные (можно позже)

---

## Заключение

AI получает данные из **двух источников**:

1. ✅ **Явные данные** - то, что мы передаем в промпт (всегда актуально)
2. ✅ **Внутренние знания** - то, что модель знает из обучения (до определенной даты)

Для улучшения прогнозов можно:
- Расширить явные данные (orderbook, объемы)
- Добавить новости через внешние API
- Использовать исторические паттерны

**Главное ограничение**: AI не знает свежие новости после даты обучения, если мы их не передаем явно.

Хотите, чтобы я добавил интеграцию с новостным API или расширил данные в промпте?

