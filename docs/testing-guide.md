# Руководство по тестированию AI интеграции

## 🧪 Быстрые тесты

### 1. Тест Tavily API

```bash
npm run test:tavily
```

**Что проверяет:**
- ✅ Быстрый поиск (quickSearch)
- ✅ Глубокий поиск (deepSearch) 
- ✅ Форматирование результатов для AI промптов
- ✅ Получение готового ответа от Tavily

**Требования:**
- `TAVILY_API_KEY` в `.env`

---

### 2. Тест AI Market Filter (полная интеграция)

```bash
npm run test:ai-filter
```

**Что проверяет:**
- ✅ AI фильтрация рынков
- ✅ Использование SerpAPI для новостей
- ✅ Использование Tavily для топ-рынков (attractiveness >= 0.75)
- ✅ Генерация AI анализа с контекстом

**Требования:**
- `OPENAI_API_KEY` или `GEMINI_API_KEY` в `.env`
- `SERP_API_KEY` в `.env` (опционально, для новостей)
- `TAVILY_API_KEY` в `.env` (опционально, для глубокого анализа)

---

### 3. Тест AI бота (полный цикл)

```bash
npm run start:ai
```

**Что проверяет:**
- ✅ Полный цикл работы AI бота
- ✅ Фильтрация рынков через AI
- ✅ Генерация сигналов
- ✅ Использование Tavily для важных рынков

---

## 📊 Что смотреть в логах

### Успешная интеграция Tavily:

```
✅ SerpAPI service initialized (news enabled)
✅ Tavily service initialized (deep analysis enabled)
📰 Found 5 recent news articles for analysis
🔬 Tavily: Deep analysis with 10 sources
🤖 AI analyzing 10 markets...
✅ AI selected 3 markets
```

### Tavily используется для топ-рынков:

- Если рынок имеет высокий attractiveness (>= 0.75), вы увидите:
  ```
  🔬 Tavily: Deep analysis with X sources
  ```

### Если Tavily не настроен:

- Система продолжит работать только с SerpAPI:
  ```
  ✅ SerpAPI service initialized (news enabled)
  ⚠️  Tavily initialization failed, continuing without deep analysis
  ```

---

## 🔍 Проверка работы Tavily в реальном боте

1. **Запустите бота:**
   ```bash
   npm run start:ai
   ```

2. **Смотрите логи:**
   - Ищите строки с `🔬 Tavily:` - это означает что Tavily используется
   - Проверьте что для топ-рынков появляется глубокий анализ

3. **Проверьте качество анализа:**
   - AI должен получать более детальную информацию
   - Reasoning должен быть более подробным для топ-рынков

---

## ⚠️ Устранение проблем

### Tavily не работает:

1. **Проверьте ключ:**
   ```bash
   grep TAVILY_API_KEY .env
   ```

2. **Проверьте тест:**
   ```bash
   npm run test:tavily
   ```

3. **Проверьте логи бота:**
   - Должна быть строка: `✅ Tavily service initialized`
   - Если нет - проверьте ключ или подключение к интернету

### Tavily не используется для рынков:

- Убедитесь что рынки имеют достаточную attractiveness (>= 0.75)
- Проверьте конфигурацию: `AI_STRATEGY_CONFIG.TAVILY_ATTRACTIVENESS_THRESHOLD`

---

## 📝 Примеры успешного вывода

### Тест Tavily:
```
🧪 Testing Tavily Integration

📊 Test 1: Quick Search
✅ Found 5 results
Response time: 0.94ms

🔬 Test 2: Deep Search  
✅ Found 10 results
📝 Tavily Answer: [готовый ответ от Tavily]

✅ All Tavily tests passed!
```

### AI Filter с Tavily:
```
🤖 AI analyzing 20 markets...
📰 Found 5 recent news articles for analysis
🔬 Tavily: Deep analysis with 10 sources
✅ AI selected 5 markets
💰 Spent: $0.025 (Total today: $0.15/$5.00)
```

