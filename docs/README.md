# Polymarket Trading Bot

Автоматизированный торговый бот для [Polymarket](https://polymarket.com) — платформы prediction markets на блокчейне Polygon.

## 🎯 Что делает бот

Polymarket Trading Bot — это комплексная система для автоматической торговли на платформе Polymarket с поддержкой:

- **🤖 AI-стратегий** — анализ рынков с использованием OpenAI/Gemini и новостей
- **🛡️ Endgame хеджирования** — защита от "черного лебедя" для высоковероятных рынков
- **📊 Мониторинг позиций** — REST API и Telegram уведомления
- **👥 Copy Trading** — отслеживание и копирование сделок успешных трейдеров
- **🔌 MCP интеграция** — Model Context Protocol для подключения к внешним источникам данных
- **🤖 Event Agents** — специализированные агенты для спорта, политики и крипто
- **🔒 Безопасный Workflow** — ручная торговля через API без автоматических стратегий
- **💾 База данных** — хранение истории торговли и статистики

## 🚀 Быстрый старт

```bash
# Установка
npm install

# Настройка .env
cp .env.example .env

# Проверка балансов
npm run check-balance

# Запуск бота
npm start              # Endgame стратегия
npm run start:ai       # AI стратегия
```

Подробнее: [Быстрый старт](getting-started.md)

## 📚 Документация

### Основные разделы

- **[Быстрый старт](getting-started.md)** — установка и настройка
- **[Стратегии торговли](strategies.md)** — обзор всех стратегий
- **[API и интеграции](api.md)** — REST API, Telegram, Copy Trading
- **[Архитектура](architecture-bot-vs-strategy.md)** — структура системы
- **[Разработка](development.md)** — руководство для разработчиков

### Стратегии

- **[Endgame Strategy](endgame-strategy.md)** — хедж-стратегия для 95-99% рынков
- **[AI Strategy](ai-usage.md)** — AI анализ с новостями
- **[High Confidence](strategies.md#high-confidence-strategy)** — упрощенная стратегия

### Сервисы и модули

- **[Адаптеры](adapters.md)** — адаптеры для внешних API
- **[Event Agents](agents.md)** — специализированные агенты (Sports, Politics, Crypto)
- **[MCP интеграция](agents-mcp-servers.md)** — Model Context Protocol серверы
- **[MarketFilter](market-filter.md)** — фильтрация и сортировка рынков
- **[Сервисы](services.md)** — обзор всех сервисов
- **[База данных](database.md)** — PostgreSQL схема и миграции

### Безопасность и Workflow

- **[Безопасный Workflow](safe-trading-workflow.md)** — ручная торговля через API

### Дополнительно

- **[Troubleshooting](troubleshooting.md)** — решение проблем
- **[Deployment](gcp-deployment.md)** — деплой на GCP
- **[Ресурсы](resources.md)** — полезные ссылки

---

## Быстрый старт

### 1. Установка
```bash
npm install
```

### 2. Настройка .env
```bash
cp .env.example .env
```

**Обязательные параметры:**
```bash
PK=ваш_приватный_ключ_без_0x
FUNDER_ADDRESS=0xВашАдрес
CHAIN_ID=137
SIGNATURE_TYPE=0
```

**AI провайдер (один из двух):**
```bash
OPENAI_API_KEY=sk-...        # Рекомендуется
GEMINI_API_KEY=...           # Альтернатива
```

**Опциональные (улучшают AI):**
```bash
SERP_API_KEY=...             # Новости за 24 часа
TAVILY_API_KEY=...           # Глубокий анализ топ-рынков (для MCP)
BRAVE_API_KEY=...            # Brave Search API (для MCP)
DATABASE_URL=postgres://...   # PostgreSQL для хранения данных
```

**MCP серверы (опционально):**
```bash
# Для Event Agents (автоматически определяются)
TAVILY_API_KEY=...           # Tavily Search MCP
BRAVE_API_KEY=...            # Brave Search MCP
ALPHAVANTAGE_API_KEY=...     # Alpha Vantage MCP
COINGECKO_PRO_API_KEY=...    # CoinGecko Pro MCP
```

### 3. Проверка и запуск
```bash
npm run check-balance    # Проверка MATIC и USDC.e
npm run trade           # Тестовая торговля (устанавливает allowance)
npm start               # Endgame бот
npm run start:ai        # AI бот
```

### Требования
- **VPN** — CLOB API недоступен из некоторых регионов
- **MATIC** для gas (минимум 0.1)
- **USDC.e** для торговли (минимум 10)
  - Адрес: `0x2791Bca1f2de4661ED88A30C99A7a9449Aa84174`

---

## Стратегии торговли

### 1. AI Strategy (`npm run start:ai`)

AI-powered стратегия с использованием OpenAI/Gemini для анализа рынков.

**Как работает:**
1. Загружает активные рынки с Polymarket
2. Базовая фильтрация: цена, категории, NegRisk
3. Проверка ликвидности через orderbook API
4. AI анализ отобранных рынков:
   - Оценка вероятности исхода
   - Анализ новостей (если включен SERP_API_KEY)
   - Расчет edge (разница между AI оценкой и рыночной ценой)
5. Генерация сигналов для рынков с достаточным edge

**Конфигурация (в `src/core/config.ts`):**
```typescript
AI_STRATEGY_CONFIG = {
  MIN_PRICE: 0.05,              // Мин цена YES (5%)
  MAX_PRICE: 0.75,              // Макс цена YES (75%)
  MIN_AI_ATTRACTIVENESS: 0.6,   // Мин привлекательность от AI
  MAX_AI_RISK: 'medium',        // Макс уровень риска
  MIN_EDGE_PERCENTAGE_POINTS: 0.10,  // Мин edge (10 п.п.)
  MAX_AI_BUDGET_PER_DAY: 5.0,   // Макс расход на AI в день ($)
  MAX_AI_BUDGET_PER_CYCLE: 0.5, // Макс расход за цикл ($)
  MIN_LIQUIDITY: 1000,          // Мин ликвидность ($)
}
```

**Файлы:**
- [src/strategies/AIStrategy.ts](../src/strategies/AIStrategy.ts) — основная логика
- [src/services/ai/ai-market-filter.ts](../src/services/ai/ai-market-filter.ts) — AI фильтрация
- [src/bot-ai.ts](../src/bot-ai.ts) — точка входа

---

### 2. Endgame Strategy (`npm start`)

Хедж-стратегия для рынков с высокой вероятностью (95-99%) близких к разрешению.

**Как работает:**
1. Находит рынки с YES 95-99%
2. Проверяет близость к дате разрешения (< 14 дней)
3. Размещает основную позицию в YES
4. Размещает хедж в NO (страховка от "черного лебедя")

**Пример для YES @ 97%, капитал $100:**
```
Основная позиция:
  YES: 103 токена @ $0.97 = $99.91

Хедж (страховка):
  NO: 83 токена @ $0.03 = $2.49

Сценарии:
  ✅ YES выигрывает (97% вероятность): +$0.60 ROI
  ❌ NO выигрывает (3% вероятность): -$19.40 (защита хеджем)
```

**Конфигурация:**
```typescript
EndgameConfig = {
  minProbability: 0.95,        // Мин вероятность YES
  maxProbability: 0.99,        // Макс вероятность YES
  maxDaysToResolution: 14,     // Макс дней до разрешения
  maxAcceptableLoss: 0.20,     // Макс убыток (20%)
  earlyExitThreshold: 0.99,    // Порог раннего выхода
  minLiquidity: 100,           // Мин ликвидность
}
```

**Файлы:**
- [src/strategies/EndgameStrategy.ts](../src/strategies/EndgameStrategy.ts)
- [src/index.ts](../src/index.ts) — точка входа

---

### 3. High Confidence Strategy (`npm run start:high`)

Упрощенная стратегия для рынков с высокой уверенностью.

**Файлы:**
- [src/strategies/HighConfidenceStrategy.ts](../src/strategies/HighConfidenceStrategy.ts)
- [src/bot-high-confidence.ts](../src/bot-high-confidence.ts)

---

### 4. Base Strategy

Базовый класс для всех стратегий. Определяет интерфейс:
- `filterMarkets()` — фильтрация рынков
- `generateSignals()` — генерация торговых сигналов
- `shouldClosePosition()` — логика закрытия позиций

**Файл:** [src/strategies/BaseStrategy.ts](../src/strategies/BaseStrategy.ts)

---

## Все команды

### Торговые боты
| Команда | Описание |
|---------|----------|
| `npm start` | Endgame бот (95-99% рынки с хеджем) |
| `npm run start:ai` | AI бот с анализом новостей |
| `npm run start:high` | High Confidence бот |
| `npm run start:api` | REST API сервер (порт 3001) - **безопасный режим** |

**⚠️ Важно:** API сервер (`npm run start:api`) **НЕ запускает автоматические стратегии**. Все действия выполняются вручную через API endpoints. См. [Безопасный Workflow](safe-trading-workflow.md).

### Мониторинг
| Команда | Описание |
|---------|----------|
| `npm run positions` | Текущие позиции, ордера, балансы |
| `npm run check-balance` | Балансы MATIC и USDC.e |

### Тесты и анализ
| Команда | Описание |
|---------|----------|
| `npm run test:api` | Проверка подключения к Polymarket |
| `npm run test:endgame` | Анализ возможностей Endgame |
| `npm run test:liquidity` | Анализ ликвидности рынков |
| `npm run test:volume` | Отладка структуры API данных |
| `npm run test:market-filter` | Тест модуля фильтрации |
| `npm run test:enriched-filter` | Тест интеграции MarketFilter + DataService |
| `npm run test:ai-filter` | Тест AI фильтрации |
| `npm run test:ai-probability` | Тест AI оценки вероятностей |
| `npm run test:ai-prompt-fix` | Тест исправлений промптов AI |
| `npm run test:tavily` | Тест Tavily API |
| `npm run test:telegram` | Тест Telegram интеграции |
| `npm run test:mcp` | Тест MCP реестра и серверов |

### Copy Trading
| Команда | Описание |
|---------|----------|
| `npm run check-wallets` | Проверить все отслеживаемые кошельки |
| `npm run add-wallet` | Добавить кошелек для отслеживания |
| `npm run list-wallets` | Список отслеживаемых кошельков |
| `npm run remove-wallet` | Удалить кошелек |

### База данных
| Команда | Описание |
|---------|----------|
| `npm run db:migrate` | Применить миграции |
| `npm run db:status` | Статус миграций |
| `npm run db:rollback` | Откатить последнюю миграцию |

### Утилиты
| Команда | Описание |
|---------|----------|
| `npm run trade` | Тестовая торговля |
| `npm run verify` | Проверка настроек |
| `npm run generate-wallet` | Генерация нового кошелька |
| `npm run create-api-keys` | Создание API ключей |
| `npm run build` | Компиляция TypeScript |
| `npm run lint` | Проверка кода |
| `npm run lint:fix` | Исправление lint ошибок |

---

## Copy Trading

Система отслеживания сделок успешных трейдеров.

### Как работает
1. Добавляете адреса успешных трейдеров в мониторинг
2. Бот периодически проверяет их сделки через Polygonscan
3. При обнаружении сделки генерирует сигнал FOLLOW/IGNORE
4. Сигналы сохраняются в БД и/или JSON файл

### Использование
```bash
# Добавить кошелек
npm run add-wallet -- 0x1234...abcd "Whale Trader"

# Список кошельков
npm run list-wallets

# Проверить сделки
npm run check-wallets

# Удалить кошелек
npm run remove-wallet -- 0x1234...abcd
```

### Структура
```
src/copy-trading/
├── index.ts                    # Точка входа
├── config.ts                   # Конфигурация
├── types.ts                    # Типы данных
├── services/
│   ├── WalletMonitor.ts        # Мониторинг кошельков
│   ├── TradeAnalyzer.ts        # Анализ сделок
│   └── SignalEmitter.ts        # Генерация сигналов
├── storage/
│   └── WalletStore.ts          # Хранение кошельков
└── scripts/
    ├── add-wallet.ts
    ├── list-wallets.ts
    └── remove-wallet.ts
```

### Сигналы
```typescript
type CopySignal = {
  action: 'FOLLOW' | 'IGNORE';
  wallet: string;
  walletName?: string;
  trade: {
    side: 'BUY' | 'SELL';
    outcome: 'Yes' | 'No';
    price: number;
    question: string;
  };
  confidence: number;      // 0-1
  suggestedSize?: number;  // Рекомендуемый размер
  maxPrice?: number;       // Макс цена для входа
  reasons: string[];
};
```

---

## REST API

REST API для мониторинга и управления ботом.

### Запуск
```bash
npm run start:api
```

Сервер запускается на порту 3001. Swagger UI доступен по адресу: `http://localhost:3001/api-docs`

### Endpoints

#### Polymarket API (реальное время)
| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/api/positions/orders` | Открытые ордера |
| GET | `/api/positions/trades` | История сделок |
| GET | `/api/positions/active` | Активные позиции |
| GET | `/api/positions/balance` | USDC баланс |
| GET | `/api/positions/summary` | Полная сводка |
| DELETE | `/api/positions/orders/:id` | Отменить ордер |
| DELETE | `/api/positions/orders` | Отменить все ордера |

#### Database API (история)
| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/api/positions/db/health` | Статус БД |
| GET | `/api/positions/db/orders` | Ордера из БД |
| GET | `/api/positions/db/trades` | Сделки из БД |
| GET | `/api/positions/db/positions` | Позиции из БД |
| GET | `/api/positions/db/signals` | Торговые сигналы |
| GET | `/api/positions/db/stats` | Статистика бота |

### Примеры

```bash
# Получить баланс
curl http://localhost:3001/api/positions/balance

# Получить открытые ордера
curl http://localhost:3001/api/positions/orders

# Получить статистику за 7 дней
curl "http://localhost:3001/api/positions/db/stats?days=7"

# Получить сигналы copy trading
curl "http://localhost:3001/api/positions/db/signals?source=copy"
```

### Файлы
- [src/start-api.ts](../src/start-api.ts) — точка входа
- [src/api/routes/positions.routes.ts](../src/api/routes/positions.routes.ts) — маршруты
- [src/api/controllers/positions.controller.ts](../src/api/controllers/positions.controller.ts) — контроллер

---

## База данных

PostgreSQL база данных для хранения истории торговли, сигналов и статистики.

### Настройка

1. Создайте PostgreSQL базу данных (локально или в облаке)
2. Добавьте `DATABASE_URL` в `.env`:
```bash
DATABASE_URL=postgres://user:password@host:5432/polymarketdb
```
3. Примените миграции:
```bash
npm run db:migrate
```

### Схема данных

#### markets
Кэш данных о рынках Polymarket.
```sql
- condition_id: уникальный ID рынка
- question: вопрос рынка
- active, closed, archived: статусы
- volume, liquidity: финансовые метрики
- tokens: JSON с токенами YES/NO
```

#### wallets
Отслеживаемые кошельки для copy trading.
```sql
- address: адрес кошелька
- name: название
- status: active/paused
- total_trades, winning_trades, roi: статистика
```

#### trades
Все сделки (свои + copy trading).
```sql
- source: bot/copy/manual
- side: BUY/SELL
- outcome: Yes/No
- price, size, notional, fee
- trade_timestamp
```

#### positions
Открытые и закрытые позиции.
```sql
- token_id, condition_id
- size, average_price
- current_price, unrealized_pnl, realized_pnl
- status: open/closed/partial
- strategy: название стратегии
```

#### orders
Размещенные ордера.
```sql
- order_id: ID от Polymarket
- side, price, size, filled_size
- status: pending/filled/partial/cancelled
- strategy: название стратегии
```

#### signals
Торговые сигналы (copy trading + AI).
```sql
- source: copy/ai/strategy
- action: BUY/SELL/FOLLOW/IGNORE
- confidence: 0-1
- suggested_price, suggested_size
- reasons: массив причин
- executed: выполнен ли
```

#### ai_analysis
Кэш AI анализа рынков.
```sql
- ai_probability: оценка AI
- market_price: рыночная цена
- edge: разница
- attractiveness, risk_level
- reasoning, news_summary
- cost_usd: стоимость запроса
```

#### bot_stats
Ежедневная статистика бота.
```sql
- date, strategy
- total_trades, winning_trades, losing_trades
- total_volume, total_pnl, total_fees
- ai_calls, ai_cost_usd
```

### Файлы
- [src/database/PostgresAdapter.ts](../src/database/PostgresAdapter.ts) — адаптер БД
- [src/database/migrations/001_initial.sql](../src/database/migrations/001_initial.sql) — схема
- [src/database/migrate.ts](../src/database/migrate.ts) — скрипт миграций

---

## Архитектура

```
polymarket_bot/
├── src/
│   ├── index.ts                 # Endgame бот
│   ├── bot-ai.ts                # AI бот
│   ├── bot-high-confidence.ts   # High Confidence бот
│   ├── start-api.ts             # REST API сервер
│   │
│   ├── strategies/              # Торговые стратегии
│   │   ├── BaseStrategy.ts
│   │   ├── AIStrategy.ts
│   │   ├── EndgameStrategy.ts
│   │   └── HighConfidenceStrategy.ts
│   │
│   ├── agents/                   # Event Agents (MCP интеграция)
│   │   ├── BaseEventAgent.ts     # Базовый класс с MCP
│   │   ├── SportsAgent.ts        # Спортивные рынки
│   │   ├── PoliticsAgent.ts      # Политические рынки
│   │   ├── CryptoAgent.ts        # Крипто рынки
│   │   ├── MCPRegistry.ts        # Реестр MCP серверов
│   │   └── AgentRegistry.ts      # Реестр агентов
│   │
│   ├── adapters/                # Адаптеры для внешних API
│   │   ├── index.ts                  # Экспорт всех адаптеров
│   │   ├── polymarket-data.adapter.ts # Данные Polymarket (orderbook, ликвидность)
│   │   ├── gamma-api.ts              # Gamma Markets API
│   │   ├── serp-api.adapter.ts       # SerpAPI (Google Search)
│   │   ├── tavily.adapter.ts         # Tavily Search API
│   │   └── telegram.adapter.ts       # Telegram Bot API
│   │
│   ├── services/                # Сервисы
│   │   ├── MarketFilter.ts           # Фильтрация рынков
│   │   ├── TelegramBot.ts            # Telegram бот (команды)
│   │   └── ai/                       # AI сервисы
│   │       ├── ai-market-filter.ts
│   │       └── ai-client.ts
│   │
│   ├── api/                     # REST API
│   │   ├── routes/
│   │   └── controllers/
│   │
│   ├── copy-trading/            # Copy Trading
│   │   ├── services/
│   │   ├── storage/
│   │   └── scripts/
│   │
│   ├── database/                # База данных
│   │   ├── PostgresAdapter.ts
│   │   ├── migrations/
│   │   └── migrate.ts
│   │
│   ├── core/                    # Ядро
│   │   └── config.ts            # Конфигурация
│   │
│   ├── types/                   # TypeScript типы
│   ├── utils/                   # Утилиты
│   └── test/                    # Тесты
│
├── scripts/                     # Скрипты утилит
├── docs/                        # Документация
└── data/                        # Данные (кошельки, сигналы)
```

### Потоки данных

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│  Polymarket API │     │   SerpAPI       │     │   Tavily API    │
│  (CLOB + Gamma) │     │   (новости)     │     │   (AI поиск)    │
└────────┬────────┘     └────────┬────────┘     └────────┬────────┘
         │                       │                       │
         │                       ▼                       ▼
         │              ┌─────────────────────────────────────────┐
         │              │            ADAPTERS (src/adapters/)      │
         │              │  PolymarketDataAdapter, SerpApiAdapter,  │
         │              │  TavilyAdapter, TelegramAdapter          │
         │              └─────────────────┬───────────────────────┘
         │                                │
         ▼                                ▼
┌─────────────────┐              ┌──────────────────┐
│PolymarketData   │─────────────▶│   MarketFilter   │
│   Adapter       │              └────────┬─────────┘
└─────────────────┘                       │
                                          ▼
                             ┌────────────────────────┐
                             │     AI Strategy /      │
                             │   Endgame Strategy /   │
                             │   Event Agents (MCP)   │
                             └────────────┬───────────┘
                                          │
                             ┌────────────▼───────────┐
                             │    Trade Signals       │
                             └────────────┬───────────┘
                                          │
         ┌────────────────────────────────┼────────────────────────────────┐
         ▼                                ▼                                ▼
┌─────────────────┐              ┌─────────────────┐              ┌─────────────────┐
│   Order Book    │              │    Database     │              │    REST API     │
│   (Polymarket)  │              │   (PostgreSQL)  │              │   (Express)     │
└─────────────────┘              └─────────────────┘              └─────────────────┘
                                          │
                                          ▼
                              ┌───────────────────────┐
                              │   MCP Servers         │
                              │   (Tavily, Brave,     │
                              │    CoinGecko, etc.)   │
                              └───────────────────────┘
```

---

## Troubleshooting

| Проблема | Решение |
|----------|---------|
| `ECONNRESET` | Включите VPN |
| `not enough balance/allowance` | Запустите `npm run trade` |
| `Could not create api key` | Установите `SIGNATURE_TYPE=0` |
| Все рынки $0 volume | API не возвращает volume, используйте `npm run test:liquidity` |
| `Database not available` | Проверьте `DATABASE_URL` и запустите `npm run db:migrate` |
| AI анализ не работает | Проверьте `OPENAI_API_KEY` или `GEMINI_API_KEY` |

---

## Важные адреса

- **USDC.e:** `0x2791Bca1f2de4661ED88A30C99A7a9449Aa84174`
- **CTF Exchange:** `0x4bFb41d5B3570DeFd03C39a9A4D8dE6Bd8B8982E`
- **Polymarket CLOB:** `https://clob.polymarket.com`

---

## Ресурсы

- [Polymarket CLOB Docs](https://docs.polymarket.com/developers/CLOB/introduction)
- [CLOB Client GitHub](https://github.com/Polymarket/clob-client)
- [Polymarket App](https://polymarket.com)
