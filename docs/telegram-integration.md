# Telegram Integration

Интеграция Telegram бота для мониторинга и управления торговым ботом через Telegram.

## Настройка

### 1. Создание Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Сохраните полученный токен (например: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2. Получение Chat ID

1. Найдите вашего бота в Telegram (по имени, которое вы указали)
2. Отправьте боту любое сообщение (например: `/start`)
3. Откройте в браузере: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
4. Найдите в ответе `"chat":{"id":123456789}` - это ваш Chat ID

### 3. Настройка переменных окружения

Добавьте в `.env` файл:

```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here
```

## Использование

### Команды бота

После настройки вы можете отправлять команды боту в Telegram:

- `/start` или `/help` - показать список команд
- `/status` - статус бота и общая информация
- `/balance` - текущий баланс USDC
- `/positions` или `/pos` - активные позиции
- `/orders` - открытые ордера
- `/trades [N]` - последние сделки (по умолчанию 10)

### Автоматические уведомления

Бот автоматически отправляет уведомления о:

- ✅ **Размещении ордеров** - когда ордер успешно размещен
- ❌ **Ошибках ордеров** - когда произошла ошибка при размещении
- 💰 **Закрытии позиций** - когда позиция закрыта (с P&L)
- 🔄 **Исполнении сделок** - когда сделка исполнена

### Webhook для команд

Если вы запускаете API сервер (`npm run start:api`), Telegram webhook доступен по адресу:

```
POST /api/telegram/webhook
```

Для настройки webhook в Telegram:

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
  -d "url=https://your-domain.com/api/telegram/webhook"
```

## Интеграция в боты

Telegram интеграция автоматически активируется при наличии переменных окружения:

- **bot-ai.ts** - AI бот с нотификациями
- **bot-high-confidence.ts** - High Confidence бот с нотификациями
- **start-api.ts** - API сервер с webhook для команд

## Примеры уведомлений

### Уведомление о размещении ордера

```
✅ Ордер размещен

🔹 BUY Yes
💰 Цена: 85.50%
📊 Размер: 1
🆔 ID: 0x1234567890abc...
📋 Рынок: Will Bitcoin reach $100k...
```

### Уведомление о закрытии позиции

```
💰 Позиция закрыта

📈 P&L: +2.50 USDC
📋 Рынок: Will Bitcoin reach $100k...
ℹ️ Причина: Профит: 95.00%
```

## Безопасность

- Бот проверяет Chat ID перед обработкой команд
- Только разрешенные чаты могут отправлять команды
- Webhook endpoint не требует аутентификации (используйте firewall или nginx для защиты)

## Troubleshooting

### Бот не отвечает

1. Проверьте, что `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID` установлены
2. Убедитесь, что бот запущен и подключен к Telegram
3. Проверьте логи на наличие ошибок

### Уведомления не приходят

1. Проверьте, что бот инициализирован (должно быть сообщение "✅ Telegram Bot подключен")
2. Убедитесь, что Chat ID правильный
3. Проверьте, что бот не заблокирован в Telegram

### Webhook не работает

1. Убедитесь, что ваш сервер доступен из интернета
2. Проверьте, что URL webhook правильный
3. Используйте HTTPS для production (Telegram требует HTTPS для webhook)

