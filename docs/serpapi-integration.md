# SerpAPI - Получение новостей для AI

## 🎯 Назначение

SerpAPI используется для получения свежих новостей о событиях, которые анализируются AI. Это позволяет AI использовать актуальную информацию, а не только знания до даты обучения.

---

## ⚙️ Настройка

```bash
# Получите ключ на serpapi.com и добавьте в .env:
SERP_API_KEY=your_serp_api_key_here
```

---

## 🔧 Использование

### В AIStrategy:

```typescript
const strategy = new AIStrategy({
    useAI: true,
    useNews: !!process.env.SERP_API_KEY  // Автоматически если ключ есть
});
```

### В AIMarketFilter:

```typescript
// Включается автоматически если SERP_API_KEY есть
const filter = new AIMarketFilter(true);
```

### Как работает:

1. Извлекаются ключевые слова из вопроса рынка
2. SerpAPI ищет новости за последние 24 часа
3. Новости передаются в промпт для AI
4. AI анализирует рынок с учетом свежих данных

---

## 💰 Стоимость

- **Free tier**: 100 запросов/месяц
- **Starter**: $50/месяц - 5,000 запросов

**Рекомендация:** Используйте для топ-кандидатов после базовой фильтрации, чтобы экономить запросы.

---

## 📊 Пример

```
Рынок: "Will Lakers win tonight?"
  ↓
SerpAPI: поиск "Lakers win tonight" (последние 24ч)
  ↓
Находит: "LeBron injured", "Lineup update"
  ↓
AI анализирует с новостями:
"Market at 65% seems overvalued given injury news"
  ↓
Решение: shouldTrade = false
```

---

Подробнее: [ai-usage.md](ai-usage.md)

